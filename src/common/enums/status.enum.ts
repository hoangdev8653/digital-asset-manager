export enum UserStatus {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
    BANNED = 'BANNED',
}

export enum AssetStatus {
    AVAILABLE = 'available',
    ASSIGNED = 'assigned',
    BROKEN = 'broken',
    MAINTENANCE = 'maintenance',
    LOST = 'lost',
}

export enum AssignmentStatus {
    ACTIVE = 'active',
    RETURNED = 'returned',
    EXPIRED = 'expired',
}
